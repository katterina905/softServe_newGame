function Ship ( deckNumber, direction, x, y ) {
  this.deckNumber = deckNumber,
  this.direction = direction;
  this.x = x,
  this.y = y;
};

Ship.prototype.isAlive = function () {

};

Ship.prototype.isHit = function () {

};
